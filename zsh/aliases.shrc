# ------------------------------------------------------------------------------
# 📁 .aliases.shrc — Shell Aliases for Local Dev Utilities
# ------------------------------------------------------------------------------
# Description:
#   Custom shell aliases and lightweight shell functions to streamline:
#   - Git workflows
#   - Docker and Kubernetes management
#   - Python and environment tooling
#   - File navigation, system introspection, and dev ergonomics
#
# Usage:
#   Source this file in your `.zshrc` or `.bashrc`:
#     source ~/.aliases.shrc
#
# Author: Casey A. Thayer
# Location: ~/.aliases.shrc
# ------------------------------------------------------------------------------

# ------------------------------------------------------------------------------
# 🐍 Python & Virtual Envs
# ------------------------------------------------------------------------------
alias python=python3
alias pip=pip3

# ------------------------------------------------------------------------------
# ✍️  Editor
# ------------------------------------------------------------------------------
export EDITOR="nvim"
alias vim=nvim
alias vv=nvim

# ------------------------------------------------------------------------------
# 💻 Shell & Navigation
# ------------------------------------------------------------------------------
alias ...='cd ../..'
alias c=clear
alias xx='exit'
alias rsh='exec $SHELL'
alias sz='source ~/.zshrc'
alias sb='source ~/.ct-bash.shrc'

alias cdh='cd $HOME'
alias cdm='cd $HOME/ct'
alias cdc='cd $HOME/ct/sys-config'

# ------------------------------------------------------------------------------
# 📂 File Listing
# ------------------------------------------------------------------------------
alias ls='eza -l --icons'
alias ll='eza -l -a --icons'
alias cat='bat'

# ------------------------------------------------------------------------------
# 🔐 Direnv
# ------------------------------------------------------------------------------
alias da='direnv allow'
alias dr='direnv reload'

# ------------------------------------------------------------------------------
# 🔧 Git
# ------------------------------------------------------------------------------
alias g='git'
alias ga='git add'
alias gc='git commit -m'
alias gp='git push'
alias gl='git log --oneline'
alias gst='git status'
alias gd='git diff'
alias gdc='git diff --cached'
alias gm='git commit -m'
alias gma='git commit -am'
alias gb='git branch'
alias gco='git checkout'
alias gra='git remote add'
alias grr='git remote rm'
alias gpu='git pull'
alias gf='git fetch'
alias gcl='git clone'
alias gcpr="gh pr create -w"
alias gsync="git checkout main && git pull --rebase && git fetch --prune"
alias glom="git pull origin main"
alias lg=lazygit

# ------------------------------------------------------------------------------
# 🐳 Docker
# ------------------------------------------------------------------------------
alias dkps='docker ps'
alias dkpsa='docker ps -a'
alias dkimgs='docker images'
alias dkst='docker stats'
alias dkl='docker logs -f'
alias dke='docker exec -it'
alias dkclean='docker rm $(docker ps -aq)'
alias dk-up='docker-compose up -d'
alias dk-down='docker-compose down'
alias dk-start='docker-compose start'
alias dk-stop='docker-compose stop'
alias dk-clean='docker system prune -a --volumes'

# ------------------------------------------------------------------------------
# ☸️ Kubernetes (functions used for args)
# ------------------------------------------------------------------------------
ka() { kubectl apply -f "$1"; }
kdj() { kubectl delete job "$1"; }
alias kgp='kubectl get pods'
alias kgj='kubectl get jobs'
alias kjr='kubectl get jobs | grep Running'
alias kjf='kubectl get jobs | grep Failed'


# ------------------------------------------------------------------------------
# 📊 R
# ------------------------------------------------------------------------------
alias R='R --no-restore-data --no-save'
alias r='radian'

# ------------------------------------------------------------------------------
# 🦆 DuckDB
# ------------------------------------------------------------------------------
alias duckdb='/Applications/duckdb ; exit;'


# ------------------------------------------------------------------------------
# 🛠️ Terraform
# ------------------------------------------------------------------------------
alias tf='./terraform.sh'

# ------------------------------------------------------------------------------
# 🔍 System & Network
# ------------------------------------------------------------------------------
alias ports='lsof -i -P -n | grep LISTEN'
alias ip='curl ifconfig.me'
alias devip='ipconfig getifaddr en0'
